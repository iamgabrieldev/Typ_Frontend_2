import styled from "styled-components";

export const Container = styled.aside`
  color: #fff;
  width: 180px;
  height: 200vh;
  background-color: #24527C;
  padding: 32px 24px;
  position: fixed;

  div {
    margin-top: 200px;
  }

  img {
      cursor: pointer;
      display: block;
      margin: -16px auto;
      margin-bottom: 64px;
    }

`